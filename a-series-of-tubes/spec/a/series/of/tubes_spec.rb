require 'spec_helper'

describe A::Series::Of::Tubes do
  it 'has a version number' do
    expect(A::Series::Of::Tubes::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
